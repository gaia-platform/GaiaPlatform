// RUN: %gaiat %s -- 2>&1| FileCheck  %s

ruleset test10
{
    on_update(incubator)
    {
        int tot_acres = 0;
        tot_acres += yield->crop.acres;
    }
}

// CHECK: There is more than one field that links 'incubator' and 'farmer'
