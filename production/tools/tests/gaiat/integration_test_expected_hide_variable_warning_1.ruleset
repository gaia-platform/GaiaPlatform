// RUN: %gaiat %s -- 2>&1| FileCheck  %s

ruleset test
{
    on_update(sensor)
    {
        if (sensor.value == 5)
        {
            int value = 5;
        }
    }
}

// CHECK: Local variable declaration 'value' hides a database field defined with the same name.
