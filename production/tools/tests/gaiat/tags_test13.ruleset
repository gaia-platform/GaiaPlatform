// RUN: not %gaiat %s -- 2>&1| FileCheck  %s

ruleset test13
{
    OnUpdate(S:sensor, A:actuator)
    {
        float v = S.value + A.value;
    }
}

// GAIAPLAT-799
// CHECK: (expect an error with 2nd attribute from a different table)
