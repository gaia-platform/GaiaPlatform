// RUN: not %gaiat %s -- 2>&1| FileCheck  %s

ruleset test00
{
    on_change(actuator.value)
    {
        actuator.value += isolated.age;
    }
}

// CHECK: No path between tables 'actuator' and 'isolated'
