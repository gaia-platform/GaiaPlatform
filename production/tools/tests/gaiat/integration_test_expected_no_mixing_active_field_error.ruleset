// RUN: not %gaiat %s -- 2>&1| FileCheck  %s

ruleset test
{
    on_insert(sensor)
    {
        actuator.value = @sensor.value + 5;
    }
}

// CHECK: Since 'on_insert', 'on_change', or 'on_update' was provided, specifying active fields using '@' inside the rule is not supported.
