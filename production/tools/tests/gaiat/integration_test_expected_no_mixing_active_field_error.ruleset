// RUN: not %gaiat %s -- 2>&1| FileCheck  %s

ruleset test
{
    OnInsert(sensor)
    {
        actuator.value = @sensor.value + 5;
    }
}
// CHECK: Specifying active fields in rule attributes and inside the rule is not supported.
