// RUN: not %gaiat %s -- 2>&1| FileCheck  %s

ruleset test1
{
    OnInsert(incubator)
    {
        while (farmer > 3)
        {
            farmer.acreage++;
        }
    }
}

// CHECK: invalid operands to binary expression
