// RUN: %gaiat %s -- 2>&1| FileCheck  %s

ruleset test
{
    on_insert(sensor)
    {
        int incubator = 5;
    }
}

// CHECK: Local variable declaration 'incubator' hides a database table defined with the same name.
