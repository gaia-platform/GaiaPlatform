// RUN: %gaiat %s -- 2>&1| FileCheck  %s

#include "tags_test.h"

ruleset test11
{
    OnChange(a:actuator)
    {
        if (a.value > 0.0)
        {
            float a;
            use_float(a);
        }
    }
}

// GAIAPLAT-794
// CHECK: Local variable declaration 'a' hides a tag of the same name
