// RUN: not %gaiat %s -- 2>&1| FileCheck  %s

ruleset test07 : tables (sensor.value, incubator)
{
    {
        if (@value < 5)
        {
            value = 5;
        }
    }
}

// GAIAPLAT-819 (fixed)
// CHECK: expected ')'
