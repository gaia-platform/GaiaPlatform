// RUN: not %gaiat %s -- 2>&1| FileCheck  %s

#include "tags_test.h"

ruleset test08
{
    OnInsert(I:incubator.min_temp)
    {
        use_float(I.min_temp);
    }
}

// GAIAPLAT-795
// CHECK: OnInsert attribute must be a table.
