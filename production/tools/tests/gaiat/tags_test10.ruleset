// RUN: %gaiat %s -- 2>&1| FileCheck  %s

#include "tags_test.h"

ruleset test10
{
    on_change(a:actuator)
    {
        use_float(a.value);
        float a;
        use_float(a);
    }
}

// GAIAPLAT-794
// CHECK: Local variable declaration 'a' hides a tag defined with the same name.
