// RUN: not %gaiat %s -- 2>&1| FileCheck  %s

#include "tags_test.h"

ruleset test10
{
    OnChange(a:actuator)
    {
        use_float(a.value);
        float a;
        use_float(a);
    }
}

// GAIAPLAT-794
// CHECK: (warn/error? This should give a warning.)
