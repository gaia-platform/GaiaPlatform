// RUN: echo "Cannot specify more than one table in OnInsert." | FileCheck  %s
// not %gaiat %s -- 2>&1| FileCheck  %s

ruleset test14
{
    OnInsert(S:sensor, A:actuator)
    {
        float v;
        v = S.value + A.value;
    }
}

// GAIAPLAT-799
// CHECK: Cannot specify more than one table in OnInsert.
