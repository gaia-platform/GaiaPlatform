// RUN: not %gaiat %s -- 2>&1| FileCheck  %s

ruleset test14
{
    OnInsert(S:sensor, A:actuator)
    {
        float v = S.value + A.value;
    }
}

// GAIAPLAT-799
// CHECK: Cannot identify more than one table in OnInsert.
