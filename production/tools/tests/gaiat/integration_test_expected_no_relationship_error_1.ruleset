// RUN: not %gaiat %s -- 2>&1| FileCheck  %s

ruleset test
{
    {
        if (@isolated.age < 5)
        {
            actuator.value = 5;
        }
    }
}
// CHECK: No path between 'isolated' and other tables.
