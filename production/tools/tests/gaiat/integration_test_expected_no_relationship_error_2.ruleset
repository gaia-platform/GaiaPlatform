// RUN: not %gaiat %s -- 2>&1| FileCheck  %s

ruleset test
{
    {
        if (@actuator.value < 5)
        {
            isolated.age = 5;
        }
    }
}
// CHECK: No navigation path exists between tables 'actuator' and 'isolated'. Ensure your schema has a relationship between these two tables.
