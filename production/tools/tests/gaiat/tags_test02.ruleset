// RUN: not %gaiat %s -- 2>&1| FileCheck  %s

#include "tags_test.h"

ruleset test02
{
    OnUpdate(A:actuator, A:actuator.name)
    {
        use_float(A.value);
    }
}

// CHECK: Duplicate tag A is defined.
