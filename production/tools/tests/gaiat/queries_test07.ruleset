// RUN: not %gaiat %s -- 2>&1| FileCheck  %s

// GAIAPLAT-1050
ruleset test10
{
    OnUpdate(incubator)
    {
        actuator.value += feeding.portion;
        sensor.value += acres;
    }
}
// CHECK: Multiple shortest paths between tables 'incubator' and 'feeding' exist.
