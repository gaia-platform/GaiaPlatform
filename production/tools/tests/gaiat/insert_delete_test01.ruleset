// RUN: not %gaiat %s -- 2>&1| FileCheck  %s

// TESTCASE: Use the wrong delete keyword.
ruleset test1
{
    on_insert(sensor)
    {
        sensor.remove();
    }
}

// CHECK: expected unqualified-id
